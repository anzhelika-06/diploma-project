# Дипломный проект EcoSteps

Веб-приложение для экологического трекинга с использованием Node.js, PostgreSQL, Redis и Docker.

## 🚀 Быстрый старт (одна команда!)

### Требования
- [Docker Desktop](https://www.docker.com/products/docker-desktop/) (включает Docker Compose)
- Git

### Запуск проекта
```bash
# Клонируем репозиторий
git clone https://github.com/anzhelika-06/diploma-project.git
cd diploma-project/diploma-project

# Запускаем все сервисы одной командой
# Для Linux/Mac:
./start.sh

# Для Windows:
start.bat

# Или напрямую через Docker Compose:
docker-compose up --build -d
```

**Готово!** 🎉 Все сервисы запущены и доступны:
- **Клиент (React)**: http://localhost:5173
- **API Server**: http://localhost:3001  
- **PostgreSQL**: localhost:5432
- **Redis**: localhost:6379

### Остановка проекта
```bash
docker-compose down
```

## 📁 Структура проекта

```
diploma-project/
├── diploma-project/           # Основная папка проекта
│   ├── server/               # Node.js сервер
│   │   ├── index.js         # Главный файл сервера
│   │   ├── routes/          # API маршруты
│   │   ├── utils/           # Утилиты (Redis, SessionManager)
│   │   ├── middleware/      # Middleware (rate limiting, auth)
│   │   ├── package.json     # Зависимости Node.js
│   │   ├── Dockerfile.dev   # Docker для разработки
│   │   └── Dockerfile.prod  # Docker для продакшена
│   ├── client/              # React клиент (Vite)
│   │   ├── src/            # Исходный код
│   │   └── package.json    # Зависимости клиента
│   ├── docker/              # Docker конфигурации
│   │   ├── postgres/        # PostgreSQL настройки
│   │   │   └── init.sql     # Инициализация БД
│   │   └── nginx/           # Nginx для продакшена
│   ├── docker-compose.yml      # Разработка
│   ├── docker-compose.prod.yml # Продакшен
│   ├── WEBSOCKET_SESSIONS.md   # Документация WebSocket
│   ├── REDIS_GUIDE.md          # Руководство по Redis
│   └── .env.example         # Пример переменных окружения
└── README.md                # Этот файл
```

## 🗄️ База данных

Проект использует PostgreSQL с автоматической инициализацией:
- Таблица `users` - пользователи системы
- Таблица `teams` - команды
- Таблица `success_stories` - истории успеха
- Таблица `story_categories` - категории историй
- Таблица `rankings` - рейтинги пользователей
- И другие таблицы для полного функционала

## 🔴 Redis

Redis используется для:
- **WebSocket сессий** - хранение активных подключений
- **Кэширование** - быстрый доступ к данным
- **Pub/Sub** - синхронизация между серверами (Socket.IO adapter)


## 🔌 WebSocket

Реализована система real-time обновлений через Socket.IO:
- Аутентификация пользователей
- Персональные комнаты
- Уведомления о лайках
- Онлайн статусы



## 🔧 API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация пользователя
- `POST /api/auth/login` - Вход в систему

### Истории успеха
- `GET /api/stories` - Получить все истории
- `POST /api/stories` - Создать историю
- `POST /api/stories/:id/like` - Лайкнуть историю
- `DELETE /api/stories/:id/like` - Убрать лайк

### Рейтинги
- `GET /api/rankings/users` - Рейтинг пользователей
- `GET /api/rankings/teams` - Рейтинг команд

### Статистика
- `GET /api/stats` - Общая статистика платформы
- `GET /api/online-users` - Список онлайн пользователей

### Система
- `GET /api/health` - Проверка здоровья сервера

## 🛠️ Технологии

### Backend
- **Node.js** - Runtime
- **Express.js** - Web framework
- **Socket.IO** - WebSocket для real-time
- **PostgreSQL** - Основная база данных
- **Redis** - Сессии и кэширование
- **bcrypt** - Хеширование паролей
- **JWT** - Аутентификация (планируется)

### Frontend
- **React** - UI библиотека
- **Vite** - Build tool
- **Socket.IO Client** - WebSocket клиент
- **React Router** - Маршрутизация

### DevOps
- **Docker** - Контейнеризация
- **Docker Compose** - Оркестрация
- **Nginx** - Reverse proxy (продакшен)
- **Nodemon** - Hot reload (разработка)

## 📝 Разработка

### Установка зависимостей локально
```bash
# Сервер
cd diploma-project/server
npm install

# Клиент
cd diploma-project/client
npm install
```

### Запуск без Docker
```bash
# Убедитесь, что PostgreSQL и Redis запущены
cd diploma-project/server
npm run dev

# В другом терминале
cd diploma-project/client
npm run dev
```

### Тестирование Redis
```bash
# Запустить тесты сессий
docker exec ecosteps_server node test-redis-sessions.js

# Подключиться к Redis CLI
docker exec -it ecosteps_redis redis-cli -a ecosteps_redis_password
```

### Просмотр логов
```bash
# Все сервисы
docker-compose logs -f

# Конкретный сервис
docker logs ecosteps_server -f
docker logs ecosteps_postgres -f
docker logs ecosteps_redis -f
```

## 🧪 Тестирование

### WebSocket подключение
1. Откройте клиент: http://localhost:5173
2. Перейдите на страницу "О проекте"
3. Откройте DevTools Console
4. Вы должны увидеть: `🔌 WebSocket подключен` и `✅ Аутентификация успешна`

### Проверка Redis
```bash
# Проверить онлайн пользователей
curl http://localhost:3001/api/online-users

# Проверить статистику
curl http://localhost:3001/api/stats
```

## 🔐 Безопасность

- Rate limiting на всех API endpoints
- Хеширование паролей с bcrypt
- CORS настроен для клиента
- Redis защищен паролем
- PostgreSQL изолирован в Docker сети

## 🚀 Деплой

Проект готов к деплою с использованием Docker Compose в продакшн режиме:

```bash
# Сборка и запуск
docker-compose -f docker-compose.prod.yml up -d --build

# Проверка статуса
docker-compose -f docker-compose.prod.yml ps

# Остановка
docker-compose -f docker-compose.prod.yml down
```



## 📄 Лицензия

MIT License - см. файл LICENSE для деталей.