# Дипломный проект EcoSteps

Веб-приложение для экологического трекинга с использованием Node.js, PostgreSQL и Docker.

## 🚀 Быстрый старт

### Требования
- Docker и Docker Compose
- Git

### Запуск для разработки
```bash
git clone https://github.com/anzhelika-06/diploma-project.git
cd diploma-project/diploma-project
cp .env.example .env
# Отредактируйте .env файл с вашими настройками
docker-compose up --build
```

Приложение будет доступно по адресу: http://localhost:3001

### Запуск для продакшена
```bash
docker-compose -f docker-compose.prod.yml up --build -d
```

## 📁 Структура проекта

```
diploma-project/
├── diploma-project/           # Основная папка проекта
│   ├── server/               # Node.js сервер
│   │   ├── index.js         # Главный файл сервера
│   │   ├── package.json     # Зависимости Node.js
│   │   ├── Dockerfile.dev   # Docker для разработки
│   │   └── Dockerfile.prod  # Docker для продакшена
│   ├── docker/              # Docker конфигурации
│   │   └── postgres/        # PostgreSQL настройки
│   │       └── init.sql     # Инициализация БД
│   ├── docker-compose.yml      # Разработка
│   ├── docker-compose.prod.yml # Продакшен
│   └── .env.example         # Пример переменных окружения
└── README.md                # Этот файл
```

## 🗄️ База данных

Проект использует PostgreSQL с автоматической инициализацией:
- Таблица `genders` - справочник полов
- Таблица `users` - пользователи системы
- Представление `users_view` - удобный вид для запросов

## 🔧 API Endpoints

- `GET /` - Информация о сервере
- `GET /api/health` - Проверка здоровья сервера
- `GET /api/test` - Тестовые данные

## 🛠️ Технологии

- **Backend**: Node.js, Express.js
- **База данных**: PostgreSQL 15
- **Контейнеризация**: Docker, Docker Compose
- **Среда разработки**: Nodemon для hot reload

## 📝 Разработка

### Установка зависимостей локально
```bash
cd diploma-project/server
npm install
```

### Запуск без Docker
```bash
# Убедитесь, что PostgreSQL запущен
npm run dev
```

## 🚀 Деплой

Проект готов к деплою с использованием Docker Compose в продакшн режиме.

## 📄 Лицензия

MIT License - см. файл LICENSE для деталей.